/**
 * Purpose: Layout dùng cho các màn hình công khai như đăng nhập, landingpage.
 */
function PublicLayout({ children }) {
  return <div className="min-h-screen bg-slate-950 text-white">{children}</div>;
}

export default PublicLayout;
